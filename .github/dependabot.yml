version: 2
updates:
  # Keep Ruby gems (Jekyll, plugins, transitive deps like nokogiri) patched.
  - package-ecosystem: "bundler"
    directory: "/"
    schedule:
      interval: "weekly"
    open-pull-requests-limit: 5
